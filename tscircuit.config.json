{
  "$schema": "https://cdn.jsdelivr.net/npm/@tscircuit/cli/types/tscircuit.config.schema.json",
  "mainEntrypoint": "index.ts",
  "buildCommand": "bunx tscircuit build --concurrency 4 --transpile --ignore-errors",
  "build": {
    "workerTimeoutMs": 10000000
  }
}
